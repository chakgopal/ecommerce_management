<<<<<<< HEAD


<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Sku:</strong>
  <%= @product.sku %>
</p>

<p>
  <strong>Status:</strong>
  <%= @product.status %>
</p>

<p>
  <strong>Short desc:</strong>
  <%= @product.short_desc %>
</p>

<p>
  <strong>Long desc:</strong>
  <%= @product.long_desc %>
</p>

<p>
  <strong>Price:</strong>
  <%= @product.price %>
</p>


<p>
  <% if @product.images.attached? %>
    <% @product.images.each do |product_image| %>

      <%= image_tag url_for(product_image.variant(resize:"400x760").processed) %>
    <% end %>
  <% end %>

</p>
<% if !seller_signed_in? || !admin_signed_in? %>
     <% if @in_cart.present? %>
        <%= button_to "Buy Now", 
          checkout_orders_path, 
          :params =>{id:@product.id},
          :class => "buy_now", 
          :method => :get %> 

         <%= button_to "Go to cart",
         show_cart_quotes_path,
         :class => "go_to_cart",
         :method => :get  %>
     <% else %>
        <%= button_to "Add To Cart", 
         new_quote_quotes_url, 
         :params =>{id:@product.id},
         :class => "add_to_cart",
         :method => :get %>

         <%= button_to "Buy Now", 
          checkout_orders_path, 
          :params =>{id:@product.id},
          :class => "buy_now", 
          :method => :get %>
     <% end %>
  <% end %>
<% if seller_signed_in? || admin_signed_in? %>
  <%= link_to 'Edit', edit_product_path(@product) %> |
=======
<% if admin_signed_in? || seller_signed_in? %>
  <%= render "back_show" %>
<% else %>
  <%= render "front_show" %>
>>>>>>> fd74ded663917e2068024f3adc91e22a5a0d436f
<% end %>
